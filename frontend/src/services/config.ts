/**
 * Shared configuration — resolves API base URL from Vite env variables.
 *
 * In development:  defaults to http://localhost:8000
 * In production:   set VITE_API_BASE_URL when building (e.g. https://api.spikeprimevirtual.com)
 */

export const API_BASE_URL: string = 'https://spikeprime-backend-902850923189.us-central1.run.app';
